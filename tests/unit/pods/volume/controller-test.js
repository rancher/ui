import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:volume', 'VolumeController', {
  needs: ['controller:application']
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
