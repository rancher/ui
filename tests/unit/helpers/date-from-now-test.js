import {
  dateFromNow
} from 'ui/helpers/date-from-now';

module('DateFromNowHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateFromNow(42);
  ok(result);
});
