import {
  dateCalendar
} from 'ui/helpers/date-calendar';

module('DateCalendarHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateCalendar(42);
  ok(result);
});
