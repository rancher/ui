<section class="header clearfix">
  <div class="pull-left">
    <h1 class="vertical-middle">
      {{t "ruleDetailPage.header" appName=model.rule.displayName}}
    </h1>
  </div>
  <div class="vertical-middle"></div>
  <div class="right-buttons">
    {{badge-state model=model.rule}}
    {{action-menu
      model=model.rule
      showPrimary=false
      classNames="ml-10 pull-right"
      size="sm"
    }}
  </div>
</section>

{{#if model.rule.description}}
  {{banner-message
    color="bg-secondary mb-0 mt-10"
    message=(linkify model.rule.description)
  }}
{{/if}}

{{#if model.rule.showTransitioningMessage}}
  <div class="{{model.rule.stateColor}}">
    <p>
      {{uc-first model.rule.transitioningMessage}}
    </p>
  </div>
{{/if}}

<section>
  <div class="row banner bg-info basics mb-0">
    <div class="vertical-middle">
      <label class="acc-label vertical-middle p-0">
        {{t "generic.version"}}
      </label>
      {{model.rule.externalIdInfo.version}}
    </div>
    <div class="vertical-middle">
      <label class="acc-label vertical-middle p-0">
        {{t "generic.namespace"}}
      </label>
      {{model.rule.namespace.displayName}} {{copy-to-clipboard
                                            clipboardText=model.rule.namespace.id
                                            size="small"
                                          }}
    </div>
    <div class="vertical-middle">
      <label class="acc-label vertical-middle p-0">
        {{t "generic.created"}}
      </label>
      {{date-calendar model.rule.created}}
    </div>
  </div>
</section>

<section>
  {{#accordion-list as |al expandFn|}}
    {{resource-condition-list
      resourceType=(t "generic.istioRule")
      conditions=model.rule.status.conditions
      expandAll=al.expandAll
      expandFn=expandFn
      expandOnInit=false
    }}

    {{#accordion-list-item
       title=(t "ruleDetailPage.answers.title")
       detail=(t "ruleDetailPage.answers.detail")
       expandAll=al.expandAll
       expand=(action expandFn)
       expandOnInit=false
    }}
      {{form-key-value
        editing=false
        initialMap=model.rule.answers
        showNoneLabel=(not model.rule.valuesYaml)
        keyLabel="ruleDetailPage.answers.variable"
      }}
      {{#if model.rule.valuesYaml}}
        {{code-block
          code=model.rule.valuesYaml
          language="yaml"
        }}
      {{/if}}
    {{/accordion-list-item}}
  {{/accordion-list}}
</section>

{{istio-yaml rule=model.rule}}