<TooltipElement
  @type="tooltip-toggle-override"
  @tooltipTemplate="tooltip-static"
  @model={{tooltipModel}}
  @inlineBlock={{true}}
>
  <BtnToggle
    @toggled={{action "addOverride"}}
    @checked={{mut btnToggled}}
  />
</TooltipElement>