{{#if scope.currentProject.isSystemProject}}
  {{banner-message
    color="bg-info"
    message=(t "monitoringPage.systemProject")
  }}
{{else}}
  {{enable-monitoring
    apps=model.apps
    level="project"
  }}
{{/if}}