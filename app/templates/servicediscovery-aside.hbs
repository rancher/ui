<aside>
  <h4>Environments</h4>
  <hr/>
  <div class="links">
    {{#each env in this itemController="environment"}}
      {{#link-to "environment" env.id classNames="resource-action-hover"}}<i class="ss-globe"></i> {{env.displayName}} {{resource-actions-menu model=env choices=env.availableActions}}{{/link-to}}
    {{/each}}
  </div>
  <hr/>
  {{#link-to "environments.new" class="btn-circle-plus btn-circle-text"}}Add Environment{{/link-to}}
</aside>
