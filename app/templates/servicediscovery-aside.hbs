<aside>
  <h4>Environments</h4>
  <hr/>
  <div class="links">
    {{#each env in this itemController="environment"}}
      {{#link-to "environment" env.id}}<i class="ss-globe"></i> {{env.displayName}}{{/link-to}}
    {{/each}}
  </div>
  <hr/>
  {{#link-to "environments.new" class="btn-circle-plus btn-circle-text"}}Add Environment{{/link-to}}
</aside>
