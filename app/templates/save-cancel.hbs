<div class="footer-actions">
  {{#if saving}}
    <button class="btn btn-success btn-disabled"><i class="fa fa-spinner fa-spin"></i> Saving...</button>
  {{else}}
    <button {{action "save"}} class="btn btn-success">{{#if editing}}Save{{else}}Create{{/if}}</button>
  {{/if}}
  <button {{action "cancel"}} class="btn btn-link">{{#if cancelAsClose}}Close{{else}}Cancel{{/if}}</button>
</div>
