{{environment-header model=model}}
{{#liquid-if showServiceInfo containerless=true}}
  {{#service-addtl-info service=selectedService dismiss='dismiss'}}
  {{/service-addtl-info}}
{{/liquid-if}}
{{! The graph container isn't actually in this template because it needs to be higher up in the DOM...}}
