{{#if projects.orchestrationState.swarmReady}}
  <section class="header clearfix">
    <h1>{{t 'swarmTab.dashboard.header'}}</h1>
  </section>

  <div class="row r-mt20 r-ml20 r-mr20">
    <div class="text-center col-md-6 col-md-offset-3 gutter">
      <div class="well">
        <div class="row">
          <img height="100" src="{{app.baseAssets}}assets/images/portainer.png" />
        </div>
        <p>{{t 'swarmTab.dashboard.dashboard.content'}}</p>
        {{#if model.dashboardUrl}}
          <a {{action "openDashboard"}} class="btn btn-primary">{{t 'swarmTab.dashboard.dashboard.button'}}</a>
        {{else}}
          <button class="btn btn-primary btn-disabled">{{t 'swarmTab.dashboard.dashboard.button'}}</button>
        {{/if}}
      </div>
    </div>
  </div>
{{/if}}
