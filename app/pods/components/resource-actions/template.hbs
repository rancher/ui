{{#each item in activeActions}}
  {{action-button
    tooltip=item.tooltip
    icon=item.icon
    big=big
    colors=colors
    color=item.color
    action="clicked"
    actionArg=item.action
    altActionArg=item.altAction
  }}
{{/each}}
