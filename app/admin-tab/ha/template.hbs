<section class="header clearfix">
  <h1>{{t 'haPage.header'}}</h1>
</section>

<section>
  {{t 'haPage.setup.prefix' appName=settings.appName}}<a href="{{settings.docsBase}}/installing-rancher/installing-server/#multi-nodes" target="_blank" rel="nofollow noreferrer">{{t 'haPage.setup.link' appName=settings.appName}}</a>{{t 'haPage.setup.suffix' appName=settings.appName}}
</section>

<section class="instances">
  {{#sortable-table
      classNames="lacsso grid sortable-table"
      bulkActions=false
      paging=false
      search=false
      sortBy=sortBy
      headers=headers
      body=model
      as |member kind|
  }}
    {{#if (eq kind "row")}}
      <td>{{member.uuid}}</td>
      <td>{{member.advertiseAddress}}</td>
      <td>{{member.httpPort}}</td>
      <td>{{#if member.clustered}}{{t 'generic.yess'}}{{else}}{{t 'generic.noo'}}{{/if}}</td>
      <td>{{date-str member.heartbeat}}</td>
    {{else if (eq kind "norows")}}
      <td colspan="{{headers.length}}" class="text-center text-muted lacsso pt-20 pb-20">{{t 'processesPage.summary.noData'}}</td>
    {{/if}}
  {{/sortable-table}}
</section>
