{{#if header}}
  <section class="header has-tabs clearfix">
    <div class="pull-left"><h1>Welcome</h1></div>
  </section>
  <p>There are no hosts in this cluster yet.  Do you want to add hosts or use an existing Kubernetes installation?</p>
{{/if}}

<div class="row">
  <div class="col span-5 offset-1 text-center option option-primary {{unless canCreate 'option-disabled'}}">
    <h2>Add Hosts</h2>
    <div class="box">
      <p>{{settings.appName}} automatically deploys and manages Kubernetes.</p>
      <p>Deploy hosts with popular providers inside of {{settings.appName}}, or use any existing hosts with Docker installed.</p>
      <p>Supports any combination of virtual machines or bare-metal, running on-premise or cloud-hosted.</p>
      <p>Provides secure overlay network for containers to communicate with each other across hosts.</p>

      {{#if canCreate}}
        <div class="links" style="top: auto; bottom: 30px;">
          {{#link-to "authenticated.clusters.cluster.host-templates" projects.currentCluster.id class="btn bg-primary"}}Select{{/link-to}}
        </div>
      {{/if}}
    </div>
    <div class="bubble bg-body round p-10"><img src="{{app.baseAssets}}assets/images/environment-standard.svg" class="mt-5" /></div>
  </div>

  {{!-- Import --}}
  <div class="col span-5 text-center option {{unless canImport 'option-disabled'}}">
    <h2>Use existing Kubernetes</h2>
    <div class="box">
      <p>Deploy containers into an existing Kubernetes installation using the hosts and networking it provides.</p>
      <p>Infrastructure resources are managed outside of {{settings.appName}}.</p>

      <p>Supports on-premise installations, or hosted services like:</p>

      <div class="row">
        <div class="col span-6">
          <a class="block mb-5" target="_blank" rel="nofollow noreferrer" href="https://azure.microsoft.com/en-us/services/container-service/">
            <img src="{{app.baseAssets}}assets/images/providers/acs.svg" width="150" height="auto" /><br/>
          </a>
        </div>
        <div class="col span-6">
          <a class="block" target="_blank" rel="nofollow noreferrer" href="https://www.ibm.com/cloud-computing/bluemix/">
            <img src="{{app.baseAssets}}assets/images/providers/bluemix.svg" width="150" height="auto" /><br/>
          </a>
        </div>
      </div>
      <div class="row">
        <div class="col span-6">
          <a class="block mb-5" target="_blank" rel="nofollow noreferrer" href="https://cloud.google.com/container-engine/">
            <img src="{{app.baseAssets}}assets/images/providers/gke.svg" width="150" height="auto" />
          </a>
        </div>
        <div class="col span-6">
          <a class="block mb-5" target="_blank" rel="nofollow noreferrer" href="https://www.ubuntu.com/kubernetes">
            <img src="{{app.baseAssets}}assets/images/providers/ubuntu.svg" width="150" height="auto" />
          </a>
        </div>
      </div>

      {{#if canImport}}
        <div class="links" style="top: auto; bottom: 30px;">
          {{#link-to "authenticated.clusters.cluster.import" projects.currentCluster.id class="btn bg-primary"}}Select{{/link-to}}
        </div>
      {{/if}}
    </div>
    <div class="bubble bg-body round p-10"><img src="{{app.baseAssets}}assets/images/environment-import.svg" class="mt-5" /></div>
  </div>
</div>
