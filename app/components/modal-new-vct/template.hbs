{{cru-volume-claim-template
  model=model
  namespace=namespace
  selectNamespace=false
  mode=(if model.id "edit" "new")
  actuallySave=false
  doSave=(action "doSave")
  cancel=(action "cancel")
  workload=workload
}}
