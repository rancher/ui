<div class="protip">{{t 'modalContainerStop.protip' key=alternateLabel}}</div>
<hr/>
<div class="container-header-text">
  {{t 'modalContainerStop.header'}}:
  <div class="display-name">
    {{#each resources as |resource|}}
      {{resource.displayName}}
    {{/each}}
  </div>
</div>

<div class="row form-group">
  <div class="col-sm-12 col-md-2 form-label">
    <label class="form-control-static">{{t 'modalContainerStop.label'}}</label>
  </div>
  <div class="col-sm-12 col-md-4 input-group">
    {{input-integer classNames="form-control" value=inputTimeout style="max-width:120px;"}}
    <div class="input-group-addon">{{t 'modalContainerStop.uom'}}</div>
  </div>
</div>
<div class="row help-block r-mb20">
  <div class="col-sm-12">
    {{t 'modalContainerStop.helpText'  defaultTimeout=defaultTimeout}}
  </div>
</div>

<div class="footer-actions">
  <button {{action "stop"}} class="btn btn-danger">{{t 'modalContainerStop.button'}}</button>
  <button {{action "cancel"}} class="btn btn-link">{{t 'generic.cancel'}}</button>
</div>
