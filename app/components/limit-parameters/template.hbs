<div class="row">
  <div class="col span-2 col-inline">{{t 'reservationParameters.label'}}</div>
  <!--
  <div class="col span-3">
    <label>{{t 'reservationParameters.localStorage.labelText'}}</label>
    <div class="input-group">
      {{input-integer min="1" step="1" value=model.localStorageMb classNames="form-control" placeholder=(t 'reservationParameters.localStorage.placeholder')}}
      <div class="input-group-addon bg-default">{{t 'reservationParameters.localStorage.unit'}}</div>
    </div>
  </div>
  -->
  <div class="col span-4">
    <label>{{t 'reservationParameters.memory.labelText'}}</label>
    <div class="input-group">
      {{input-integer min="4" step="1" value=memoryLimit classNames="form-control" placeholder=(t 'reservationParameters.memory.placeholder')}}
      <div class="input-group-addon bg-default">{{t 'reservationParameters.memory.unit'}}</div>
    </div>
  </div>
  <div class="col span-4">
    <label>{{t 'reservationParameters.milliCpu.labelText'}}</label>
    <div class="input-group">
      {{input type="text" value=model.milliCpu classNames="form-control" placeholder=(t 'reservationParameters.milliCpu.placeholder')}}
      <div class="input-group-addon bg-default">{{t 'reservationParameters.milliCpu.unit'}}</div>
    </div>
  </div>
</div>
