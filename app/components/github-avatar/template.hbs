{{yield}}
{{#if avatarUrl}}
  {{#if link}}
    <a href="{{url}}" target="_blank">
      <img src="{{avatarUrl}}" width="{{size}}" height="{{size}}">
    </a>
  {{else}}
    <img src="{{avatarUrl}}" width="{{size}}" height="{{size}}">
  {{/if}}
{{else}}
  <div class="gh-placeholder"></div>
{{/if}}
