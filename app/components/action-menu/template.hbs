{{yield}}
{{#if showPrimary}}
  {{#each model.primaryActions as |act index|}}
    <button class="btn btn-default btn-{{size}}" data-primary="{{index}}" aria-label="{{act.label}}">
      {{#tooltip-element type="tooltip-basic" model=act }}
        <i class="icon-fw {{act.icon}}" alt="{{act.label}}"></i>
      {{/tooltip-element}}
    </button>
  {{/each}}
{{/if}}
<button aria-label="More Actions" aria-controls="resource-actions" class="btn btn-default btn-{{size}} more-actions" data-toggle="dropdown" data-target=".resource-actions" aria-expanded="false">
  <i class="icon icon-fw icon-vertical-ellipsis" alt="More Actions"></i>
</button>


