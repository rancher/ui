<section class="horizontal-form container-fluid">
  {{#if justCreated}}
    <h2>{{t 'editApiKey.title.justCreated'}}</h2>

    <p>
      <label>{{t 'apiPage.currentEndpoint'}}</label>
      <div class="btn bg-transparent" style="font-size: 150%;">
        <code>
          <a href="{{linkEndpoint}}" target="_blank" rel="nofollow noreferer">{{displayEndpoint}}</a>
          {{copy-to-clipboard clipboardText=linkEndpoint size="sm"}}
        </code>
      </div>
    </p>

    <p>
      <label>{{t 'editApiKey.publicValue.label'}}</label>
      <div style="font-size: 150%;">
        {{#copy-to-clipboard clipboardText=clone.publicValue}}
          <code>{{clone.publicValue}}</code>
        {{/copy-to-clipboard}}
      </div>
    </p>

    <p>
      <label>{{t 'editApiKey.secretValue.label'}}</label>
      <div style="font-size: 150%;">
        {{#copy-to-clipboard clipboardText=clone.secretValue}}
          <code>{{clone.secretValue}}</code>
        {{/copy-to-clipboard}}
      </div>
    </p>

    <div class="banner bg-warning">
      <div class="banner-icon">
        <i class="icon icon-info"></i>
      </div>
      <div class="banner-message">
        <p>{{t 'editApiKey.saveWarning1'}}<br/>{{t 'editApiKey.saveWarning2'}}</p>
      </div>
    </div>

    <div class="footer-actions">
      <button {{action "cancel"}} class="btn bg-primary">{{t 'generic.closeModal'}}</button>
    </div>
  {{else}}
    <h2>{{t (if editing 'editApiKey.title.editing' 'editApiKey.title.new')}}</h2>
    <hr/>
    {{#if editing}}
      <div class="row">
        <div class="">
          <label>{{t 'editApiKey.publicValue.label'}}</label>
          <div>{{originalModel.publicValue}}</div>
        </div>
      </div>
    {{/if}}

    {{form-name-description
      model=model
      namePlaceholder="editApiKey.name.placeholder"
      descriptionPlaceholder="editApiKey.description.placeholder"
    }}

    {{#unless editing}}
      <div class="row">
        <div class="">
          <p class="text-info">{{t 'editApiKey.noteOptional'}}</p>
        </div>
      </div>
    {{/unless}}

    {{save-cancel editing=editing save="save" cancel="cancel"}}
  {{/if}}
</section>
