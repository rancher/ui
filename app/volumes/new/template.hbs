{{cru-persistent-volume-claim
  model=model.pvc
  persistentVolumes=model.persistentVolumes
  storageClasses=model.storageClasses
  mode="new"
  actuallySave=true
  done=(action "done")
  cancel=(action "cancel")
}}
