{{view-edit-project
  project=model.project
  allProjects=model.all
  projectTemplates=availableProjectTemplates
  initialStacks=null
  showEdit=true
  editing=false
  done=(action "done")
  cancel=(action "cancel")
}}
